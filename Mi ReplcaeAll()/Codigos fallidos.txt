1. (ultimo)

public String reemplazartodo(String old, String nuevo){
        long startTime = System.nanoTime();
        String cadreem="";
        if(nuevo.length() <= 0){
            for(int i = 0; i < cad.length(); i++){
                if((cad.charAt(i)+"").equals(old)){
                    cadreem = cadreem + nuevo;
                }else{
                    cadreem= cadreem + cad.charAt(i);
                }
            }
        }else{
            String palabra ="";
            for(int i =0; i <old.length();i++){
                palabra = palabra + cad.charAt(i);
            }
            if(old.equals(palabra)){
                // eliminar palabra por palabra
                cadreem= nuevo + cad;
            }
        }
        long endTime = System.nanoTime();
        long tiempo = endTime - startTime;
        System.out.println("Tiempo: "+ tiempo);
        return cadreem;
    }

2. 

